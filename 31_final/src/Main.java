import controller.SpilController;

public class Main {
	/**
	 * Main metode, der starter spillet
	 * @param args
	 */
	public static void main(String[] args) {
		SpilController controller = new SpilController();
		controller.startGame();
	}
}
